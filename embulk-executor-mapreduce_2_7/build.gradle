sourceSets {
    main {
        java { srcDir "${project.rootDir}/embulk-executor-mapreduce/src/main/java" }
    }
    test {
        java { srcDir "${project.rootDir}/embulk-executor-mapreduce/src/test/java" }
    }
}

dependencies {
    compile 'org.apache.hadoop:hadoop-client:2.7.3'
    testRuntime 'org.embulk.executor.mapreduce:embulk-executor-mapreduce_2_7'
}
